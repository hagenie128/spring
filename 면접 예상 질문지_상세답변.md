# 키오스크 면접 예상 질문지 — 상세 답변 버전

## 사용 방법

각 답변은 면접에서 약 30초에서 1분 정도 말할 수 있도록 작성했습니다. 먼저 결론을 말하고, 이유와 실제 프로젝트 적용 사례를 붙이는 방식입니다. `[개인화 필요]` 부분에는 본인이 실제 담당한 파일·화면·수치를 넣어야 합니다.

---

# SECTION 01 · Java

## 문제 1. 객체지향 프로그래밍의 특징을 설명하세요.

**답변:** 객체지향 프로그래밍은 데이터를 단순한 값의 모음으로 보지 않고, 상태와 행동을 가진 객체들의 협력으로 문제를 해결하는 방식입니다. 캡슐화는 객체 내부 상태를 보호하고 변경 지점을 제한하는 것이고, 상속은 공통 기능을 재사용하는 것입니다. 다형성은 부모 타입이나 인터페이스로 여러 구현을 다루는 것이며, 추상화는 사용자가 알아야 할 핵심 계약만 노출하는 것입니다. 실무에서는 상속을 무조건 많이 사용하기보다, 변경 가능성이 낮고 역할이 명확한 부분에는 인터페이스와 조합을 우선 검토합니다.

## 문제 2. 오버로딩과 오버라이딩의 차이를 설명하세요.

**답변:** 오버로딩은 같은 클래스 안에서 같은 이름의 메서드를 매개변수 개수나 타입만 다르게 정의하는 것이며 컴파일 시점에 어떤 메서드를 호출할지 결정됩니다. 오버라이딩은 상속 관계에서 자식 클래스가 부모 메서드의 동작을 재정의하는 것이며 실행 시점의 실제 객체 타입에 따라 동작이 결정됩니다. 오버라이딩을 사용할 때는 부모가 약속한 동작을 깨뜨리지 않아야 하므로, 단순히 코드를 재사용하는 목적이라면 조합이 더 적절한지도 함께 검토합니다.

## 문제 3. `==`와 `equals()`의 차이를 설명하세요.

**답변:** 기본형에서 `==`는 실제 값을 비교하지만, 참조형에서 `==`는 두 변수가 같은 객체를 가리키는지 비교합니다. `equals()`는 객체의 논리적 동등성을 비교하기 위한 메서드라서, 예를 들어 주문번호나 메뉴 ID처럼 값이 같으면 같은 것으로 볼 객체는 기준을 재정의할 수 있습니다. `equals()`를 재정의하면 `hashCode()`도 같은 기준으로 재정의해야 HashMap이나 HashSet에서 일관되게 동작합니다.

## 문제 4. 예외 처리를 어떻게 설계하겠습니까?

**답변:** 먼저 정상 입력과 예상 가능한 실패를 구분하고, 서비스 계층에서는 의미 있는 도메인 예외를 발생시키겠습니다. Controller에서는 예외를 일관된 HTTP 상태 코드와 오류 코드로 변환하고, 사용자에게는 내부 스택 트레이스 대신 이해 가능한 메시지만 보여주겠습니다. 로그에는 원인과 추적 ID를 남기되 비밀번호·토큰·결제정보는 남기지 않습니다. 예외를 무조건 `catch`해서 무시하면 장애 원인을 찾을 수 없으므로, 복구할 수 있는 예외만 처리하고 나머지는 전역 처리기로 전달하겠습니다.

## 문제 5. 멀티스레드에서 발생할 수 있는 문제와 해결 방법은 무엇인가요?

**답변:** 여러 스레드가 같은 데이터를 동시에 수정하면 경쟁 상태와 데이터 불일치가 발생할 수 있고, 서로 락을 기다리면 교착 상태가 생길 수 있습니다. 공유 상태를 최소화하고 불변 객체, 원자 연산, 적절한 락 범위를 사용하겠습니다. 주문 완료나 결제 승인처럼 중복이 발생하면 안 되는 기능은 메모리의 `synchronized`만으로 해결하지 않고, 서버 재시작과 다중 인스턴스를 고려해 DB 유니크 제약과 멱등성까지 적용해야 합니다.

## 문제 6. 의존성이 높은 코드를 어떻게 개선하겠습니까?

**답변:** 의존성이 높다는 것은 한 클래스의 변경이 다른 클래스의 구현 세부사항까지 영향을 준다는 뜻입니다. 먼저 각 클래스의 책임을 확인하고, 외부 기능에 대한 인터페이스를 만든 뒤 생성자 주입으로 의존성을 명시하겠습니다. 예를 들어 주문 서비스가 직접 결제 단말과 DB 구현을 모두 생성하지 않고 결제 처리기와 저장소 인터페이스를 주입받으면 테스트에서 가짜 구현을 사용할 수 있습니다. 개선 후에는 기존 동작이 유지되는지 테스트로 확인하겠습니다.

---

# SECTION 02 · 웹·프론트엔드

## 문제 1. React에서 props와 state의 차이를 설명하세요.

**답변:** props는 부모가 자식에게 전달하는 입력값이고 자식이 직접 수정하지 않는 것이 원칙입니다. state는 컴포넌트나 전역 상태 저장소가 관리하며 값이 바뀌면 관련 UI가 다시 계산됩니다. ASAK에서는 메뉴 선택, 장바구니, 결제 진행 상태처럼 사용자 행동에 따라 변하는 값은 상태로 관리하고, 메뉴 카드에 표시할 이름이나 이미지는 props로 전달하는 식으로 책임을 나눌 수 있습니다. 상태를 여러 곳에서 중복 보관하면 서로 다른 값이 보일 수 있으므로 단일 원천을 정하는 것이 중요합니다.

## 문제 2. 컴포넌트를 분리하는 기준은 무엇인가요?

**답변:** 반복되는 UI인지, 독립적인 책임이 있는지, 별도의 상태와 이벤트 흐름을 가지는지를 기준으로 분리합니다. 예를 들어 메뉴 카드, 옵션 그룹, 장바구니 행, 주문 상태 배지는 재사용성과 테스트를 위해 분리할 수 있습니다. 반대로 단순히 JSX 몇 줄을 기계적으로 분리하면 props 전달만 늘어날 수 있으므로, 분리 후 이름만 보고도 책임을 이해할 수 있는지를 확인합니다. 공통 컴포넌트는 화면마다 다른 예외를 억지로 넣기보다 명확한 props 계약을 갖게 하겠습니다.

## 문제 3. `useEffect`는 언제 사용하나요?

**답변:** `useEffect`는 렌더링 결과를 외부 시스템과 동기화할 때 사용합니다. API 조회, 이벤트 리스너, 타이머, 실시간 연결 등이 예이고, 계산된 값을 만들거나 버튼 클릭에 따른 상태 변경을 모두 Effect로 처리하는 것은 피하겠습니다. Effect를 사용할 때는 의존성 배열을 정확히 설정하고, 컴포넌트가 사라질 때 interval·이벤트·요청을 정리해야 합니다. ASAK 관리자 주문 보드의 polling도 이전 요청과 겹치지 않게 하고 unmount 시 polling을 정리해야 합니다.

## 문제 4. 로딩·빈 상태·오류 상태를 어떻게 설계하나요?

**답변:** 화면을 단순히 데이터가 있거나 없는 두 상태로 보지 않고 `idle`, `loading`, `success`, `empty`, `error`를 구분합니다. 로딩 중에는 중복 클릭을 막고, 빈 상태에는 사용자가 다음에 무엇을 할지 안내하며, 오류 상태에는 재시도 방법을 제공합니다. 기존 데이터가 화면에 있는 상태에서 polling 한 번이 실패했다고 전체 목록을 빈 화면으로 만들면 사용자에게 데이터가 삭제된 것처럼 보일 수 있으므로 마지막 정상 데이터를 보존하는 정책도 필요합니다.

## 문제 5. 비동기 API 호출에서 중복 요청과 오래된 응답을 어떻게 막나요?

**답변:** 첫째, 버튼을 처리 중 상태로 바꾸고 동일 이벤트를 잠시 막습니다. 둘째, 서버에서 idempotency key나 상태 검증으로 최종 방어를 합니다. 검색이나 polling처럼 여러 요청이 동시에 발생할 수 있는 경우에는 AbortController, 요청 번호, `updatedAt` 비교 등으로 오래된 응답이 최신 상태를 덮어쓰지 않게 합니다. 프론트의 방어는 UX를 위한 것이고, 주문·결제의 정합성은 반드시 서버와 DB에서도 보장해야 합니다.

## 문제 6. 접근성을 개발에 어떻게 반영하겠습니까?

**답변:** 키오스크는 다양한 사용자가 이용하므로 색상만으로 상태를 구분하지 않고 텍스트와 아이콘을 함께 제공하겠습니다. 버튼의 실제 목적을 알 수 있는 문구와 접근 가능한 이름을 사용하고, 충분한 터치 영역·키보드 포커스·명도 대비·오류 메시지 위치를 확인하겠습니다. 품절 메뉴는 단순히 회색으로만 표시하지 않고 품절 상태와 선택 불가 이유를 함께 전달하겠습니다.

---

# SECTION 03 · HTTP와 REST API

## 문제 1. REST API를 설계할 때 가장 중요하게 보는 것은 무엇인가요?

**답변:** 자원 이름, HTTP 메서드, 상태 코드, 요청·응답 필드, 오류 형식이 일관적인지를 먼저 봅니다. API는 프론트와 백엔드가 함께 사용하는 계약이므로 구현자 개인의 편의보다 소비자가 예측할 수 있는 형태가 중요합니다. ASAK은 API 응답을 `{ success, status, code, message, data }` envelope로 통일하고, DB의 snake_case와 API의 camelCase를 분리하는 기준을 사용합니다. 명세만 믿지 않고 실제 Controller·DTO·Mapper와 대조해 계약이 맞는지도 확인하겠습니다.

## 문제 2. PUT과 PATCH의 차이를 설명하세요.

**답변:** PUT은 리소스 전체를 교체하는 의미이고 PATCH는 일부 속성만 변경하는 의미입니다. 관리자 주문 상태 변경처럼 상태 하나만 바꾸는 기능은 PATCH가 의미에 더 맞습니다. 다만 실제 동작은 프로젝트 계약에 따라 결정하므로, 메서드 이름만 보고 프론트와 백엔드가 다른 필드를 보내지 않도록 요청 본문과 허용 상태 전이를 명시하겠습니다.

## 문제 3. API 입력 검증은 왜 서버에서 해야 하나요?

**답변:** 클라이언트 검증은 사용자가 빠르게 피드백을 받기 위한 UX 기능일 뿐 신뢰할 수 있는 보안 경계가 아닙니다. 사용자는 직접 API를 호출하거나 요청을 변조할 수 있으므로 서버에서 타입·필수값·범위·권한·현재 DB 상태를 다시 검증해야 합니다. Spring에서는 DTO와 Bean Validation으로 형식 검증을 하고, Service에서 메뉴 품절·가격·상태 전이처럼 DB와 관련된 업무 규칙을 검증하겠습니다.

## 문제 4. 멱등성이란 무엇이고 주문에 왜 필요한가요?

**답변:** 같은 요청을 여러 번 보내도 결과가 한 번 보낸 것과 같도록 처리하는 성질입니다. 네트워크 타임아웃이 나면 클라이언트는 서버가 처리했는지 모른 채 재시도할 수 있기 때문에 주문과 결제에 중요합니다. 요청에 idempotency key를 받고, 서버가 이미 처리한 키의 결과를 반환하며, DB 유니크 제약과 상태 검증을 함께 사용하겠습니다. 완료 상태를 다시 완료하는 요청은 오류로만 처리하기보다 현재 상태를 반환하는 것도 한 방법입니다.

## 문제 5. API 보안에서 최근 중요하게 보는 항목은 무엇인가요?

**답변:** 인증 자체뿐 아니라 리소스별 인가와 객체 수준 권한 검증이 중요합니다. 사용자가 다른 주문 ID를 넣어도 조회되는지, 관리자 API를 일반 사용자가 호출할 수 있는지 확인해야 합니다. 또한 민감한 업무 흐름에 대한 무제한 반복 요청, 과도한 데이터 반환, 외부 API를 안전하지 않게 소비하는 문제도 점검합니다. ASAK에서는 주문·결제·관리자 상태 변경에 대해 ID와 역할을 서버에서 다시 검증하고, 필요한 필드만 응답하도록 하겠습니다.

---

# SECTION 04 · 데이터베이스

## 문제 1. 주문과 주문 상세를 분리하는 이유를 설명하세요.

**답변:** 주문에는 주문번호, 주문방식, 전체 금액, 상태, 생성 시각처럼 주문 전체에 한 번만 필요한 정보를 저장하고, 주문 상세에는 상품별 메뉴 ID, 수량, 단가, 옵션 정보를 저장합니다. 한 주문에 여러 상품이 들어갈 수 있으므로 주문과 주문 상세는 1:N 관계입니다. 특히 메뉴 가격이 나중에 바뀌더라도 과거 주문 금액이 달라지면 안 되므로 주문 당시 단가와 계산 금액을 상세에 보존하는 것이 중요합니다.

## 문제 2. 결제 처리에서 트랜잭션만으로 충분하지 않은 이유는 무엇인가요?

**답변:** DB 트랜잭션은 우리 DB 안의 작업을 원자적으로 묶지만, 외부 결제 단말이나 PG의 승인 결과까지 동일한 DB 트랜잭션으로 되돌릴 수는 없습니다. 따라서 승인 후 주문 저장 실패, 타임아웃 후 실제 승인은 성공한 경우 같은 보상 상황을 설계해야 합니다. 결제 상태를 `READY`, `IN_PROGRESS`, `APPROVED`, `FAILED`처럼 관리하고, 승인 조회·재처리·취소 정책과 idempotency key를 함께 두겠습니다.

## 문제 3. DB 성능이 느릴 때 어떤 순서로 확인하나요?

**답변:** 먼저 같은 조건에서 실제로 느린 요청을 재현하고 애플리케이션 구간과 DB 구간을 분리합니다. 그다음 실행 계획, 인덱스 사용 여부, 조인으로 인한 중복 행, 조회 컬럼과 범위, 정렬·LIKE 조건을 확인합니다. 인덱스를 추가하기 전에 읽기 개선과 쓰기 비용을 함께 보고, 변경 후에는 같은 데이터 규모에서 응답 시간과 실행 계획을 다시 비교하겠습니다. 운영 환경에서는 느린 쿼리와 커넥션 풀·락 대기도 함께 확인합니다.

## 문제 4. 논리 삭제와 물리 삭제 중 무엇을 선택하나요?

**답변:** 물리 삭제는 행을 실제로 제거하고 논리 삭제는 삭제 여부나 상태를 남깁니다. 주문·결제·감사 이력이 필요한 데이터는 복구와 추적을 위해 논리 삭제나 상태 보존을 우선 검토합니다. 반면 개인정보 보관 기간이나 불필요한 임시 데이터처럼 실제 삭제가 필요한 경우도 있으므로 법적 보관 정책과 참조 관계를 확인한 뒤 결정하겠습니다.

---

# SECTION 05 · Spring Boot·백엔드

## 문제 1. Spring MVC의 요청 처리 흐름을 설명하세요.

**답변:** 요청이 서버에 도착하면 DispatcherServlet이 적절한 Controller를 찾고, 요청 경로와 메서드에 맞는 핸들러를 호출합니다. 파라미터와 요청 본문은 Resolver와 MessageConverter를 통해 DTO로 변환되고, Controller는 Service를 호출합니다. Service는 검증·업무 규칙·트랜잭션을 처리하고 Repository나 Mapper가 DB를 다룹니다. 마지막으로 결과 DTO가 JSON으로 직렬화되어 상태 코드와 함께 클라이언트로 돌아갑니다.

## 문제 2. Controller와 Service에 어떤 책임을 두나요?

**답변:** Controller에는 HTTP 입력 변환, 기본 형식 검증, 응답 상태와 DTO 변환처럼 웹 계층의 책임만 둡니다. 주문 금액 계산, 품절 확인, 상태 전이, 중복 요청 판단 같은 업무 규칙은 Service에 둡니다. 이렇게 하면 같은 업무를 다른 API나 배치에서 호출할 때 재사용할 수 있고, 웹 기술 없이 서비스 로직을 테스트하기도 쉽습니다.

## 문제 3. DTO와 Entity를 분리하는 이유는 무엇인가요?

**답변:** Entity를 외부 요청에 직접 사용하면 DB 구조와 API 계약이 강하게 결합되고, 사용자가 수정하면 안 되는 필드까지 바뀔 수 있습니다. 요청 DTO에는 허용된 입력과 검증 규칙만 두고, Service에서 Entity 변경을 명시적으로 수행하겠습니다. 응답도 Entity 전체를 그대로 노출하지 않고 필요한 응답 DTO로 변환해 내부 컬럼과 민감정보를 보호하겠습니다.

## 문제 4. `@Transactional`을 사용할 때 주의할 점은 무엇인가요?

**답변:** 트랜잭션 범위가 너무 넓으면 락과 커넥션 점유가 길어지고, 외부 네트워크 호출까지 포함하면 실패와 재시도가 복잡해집니다. 반대로 범위가 너무 좁으면 주문 기본 정보와 상세 저장이 따로 커밋될 수 있습니다. DB에 함께 확정되어야 하는 작업은 하나의 서비스 경계로 묶고, 외부 결제는 승인 상태와 보상 처리로 분리하겠습니다. 프록시 기반 동작, 내부 메서드 호출, 롤백 대상 예외도 확인해야 합니다.

## 문제 5. MyBatis의 `#{}`와 `${}`를 설명하세요.

**답변:** `#{}`는 Prepared Statement 방식으로 값을 바인딩하므로 사용자 입력이 SQL 문법으로 해석되지 않습니다. `${}`는 문자열을 SQL에 그대로 넣기 때문에 컬럼명이나 정렬 방향처럼 구조적으로 제한된 값을 조합하는 경우가 아니면 위험합니다. 사용자 검색어·ID·가격·상태값에는 `#{}`를 사용하고, 동적 정렬이 필요하면 허용 목록에서 매핑한 뒤 제한적으로 사용하겠습니다.

## 문제 6. 운영 가능한 백엔드란 무엇인가요?

**답변:** 기능이 동작하는 것뿐 아니라 문제가 발생했을 때 원인을 찾고 안전하게 복구할 수 있어야 합니다. 요청 추적을 위한 correlation ID, 구조화된 로그, 핵심 지표, 오류 비율과 응답 시간, DB와 외부 결제 상태를 확인할 수 있어야 합니다. 비밀번호나 토큰은 로그에서 마스킹하고, 재시도 가능한 오류와 즉시 실패해야 하는 오류를 구분하겠습니다. 최근에는 로그·메트릭·트레이스를 함께 보는 관측 가능성도 중요하므로 API 한 건이 프론트부터 DB까지 어떻게 흘렀는지 추적할 수 있는 구조를 지향합니다.

---

# SECTION 06 · Git·협업·AI

## 문제 1. API 계약이 팀원 코드와 다르면 어떻게 해결하나요?

**답변:** 먼저 프론트의 실제 요청, 백엔드 Controller·DTO, 실행 응답, API 명세를 각각 확인합니다. URL과 HTTP 메서드, camelCase 필드, 상태 코드, 오류 envelope가 어느 지점에서 달라졌는지 표로 정리한 뒤 담당자와 기준을 합의하겠습니다. 이미 사용 중인 계약을 바꿀 때는 영향 화면과 테스트를 확인하고, 필요하면 호환 기간이나 버전 전략도 정합니다. 코드 한쪽만 임의 수정해서 잠깐 동작하게 만드는 것은 재발 가능성이 높습니다.

## 문제 2. 좋은 코드 리뷰는 무엇을 확인해야 하나요?

**답변:** 요구사항을 충족하는지, 정상 흐름과 실패 흐름이 모두 처리되는지, 권한·입력 검증·민감정보 노출 문제가 없는지 확인합니다. 그다음 기존 API 호환성, 트랜잭션 범위, 중복 요청, 성능과 테스트 가능성을 봅니다. 의견은 “좋아 보인다”보다 파일과 조건을 제시해 남기고, 수정이 필요한 문제와 취향 수준의 제안을 구분하겠습니다.

## 문제 3. AI를 개발에 어떻게 활용하겠습니까?

**답변:** AI는 요구사항을 테스트 케이스로 바꾸거나, 낯선 코드의 흐름을 설명받거나, 오류 원인 후보와 문서 초안을 만드는 보조 도구로 사용하겠습니다. 생성 코드를 바로 반영하지 않고 현재 프로젝트의 버전·구조·계약과 맞는지 확인하고, 공식 문서와 최소 재현 코드로 검증하겠습니다. 회사 코드·DB 비밀번호·토큰·개인정보는 제거하고, 최종적으로 제가 코드의 입력·출력·실패 조건을 설명할 수 있을 때만 사용하겠습니다.

## 문제 4. AI가 여러 해결책을 제시하면 무엇을 기준으로 고르나요?

**답변:** 첫째 요구사항과 현재 아키텍처에 맞는지, 둘째 보안과 데이터 정합성을 지키는지, 셋째 팀원이 유지보수할 수 있는지, 넷째 테스트로 검증 가능한지를 봅니다. 최신 기술이라는 이유만으로 선택하지 않고 도입 비용과 기존 코드와의 일관성을 비교하겠습니다. 선택한 이유와 포기한 대안을 간단한 ADR이나 주석으로 남기면 이후 팀원이 판단을 이해하기 쉽습니다.

---

# SECTION 07 · ASAK 프로젝트 상세 답변

## 문제 1. ASAK 프로젝트를 1분 이내로 설명해 주세요.

**답변:** “ASAK은 고객용 키오스크와 관리자 운영 화면을 연결한 주문 서비스입니다. 고객은 매장 식사 또는 포장을 선택하고, 메뉴 상세에서 옵션과 제외 재료를 선택한 뒤 장바구니와 결제를 진행합니다. 서버는 클라이언트가 보낸 금액을 그대로 믿지 않고 메뉴·옵션·품절 상태를 다시 확인한 뒤 가격을 계산해 주문을 저장합니다. 관리자는 주문 목록과 실시간 주문 보드에서 `RECEIVED → PREPARING → COMPLETED` 상태를 관리하고, 완료된 주문을 주방 안내와 연결할 수 있습니다. 저는 [개인화 필요: 담당 화면·API·파일]을 중심으로 데이터 흐름과 실패 상태까지 확인했습니다.”

## 문제 2. ASAK의 전체 주문 흐름을 설명해 주세요.

**답변:** “사용자는 Home에서 `orderType`을 선택한 뒤 Menu List, Menu Detail, Cart, 주문 생성, Payment, Complete 순서로 이동합니다. 장바구니에서 수량과 선택 옵션을 확인한 뒤 `POST /api/kiosk/orders`를 호출합니다. 백엔드는 항목 존재 여부, 수량, 메뉴 판매 상태, 옵션 그룹과 필수·최소·최대 선택, 옵션·재료 품절, 가격을 순서대로 검증합니다. 주문이 저장되면 `orderId`, `orderNo`, 주문·결제 상태와 서버가 계산한 금액을 반환하고 결제 화면으로 이동합니다.”

## 문제 3. 프론트에서 Spring Boot와 DB까지의 흐름을 설명해 주세요.

**답변:** “React 화면의 이벤트 핸들러가 API 모듈을 호출하고, Axios 또는 공통 apiClient가 JSON 요청을 보냅니다. 서버에서는 Controller가 요청 DTO를 받고 형식과 필수값을 검증한 뒤 Service를 호출합니다. Service는 주문 업무 규칙과 트랜잭션을 처리하고 Repository 또는 MyBatis Mapper가 DB를 조회·저장합니다. 응답은 공통 envelope로 돌아오며, 프론트는 `data`를 사용해 상태를 갱신하고 오류는 상태 코드와 오류 코드에 맞는 안내로 표시합니다.”

## 문제 4. 주문·주문 상세·결제는 어떻게 설계했나요?

**답변:** “주문 테이블에는 주문번호, 주문방식, 상태, 총액, 생성 시각처럼 주문 전체의 정보를 저장합니다. 주문 상세에는 메뉴 ID, 메뉴명 스냅샷, 수량, 주문 당시 단가, 옵션과 상세 금액을 저장해 이후 메뉴 가격이 바뀌어도 과거 주문을 재현할 수 있게 합니다. 결제는 주문 ID와 연결하고 결제수단, 승인 상태, 승인 금액, 승인 시각, 실패 사유를 별도로 관리합니다. 이렇게 분리하면 주문 완료와 결제 승인, 조리 완료를 서로 다른 상태로 정확히 표현할 수 있습니다.”

## 문제 5. 가격을 프론트에서 계산했는데 서버에서 다시 계산해야 하는 이유는 무엇인가요?

**답변:** “프론트의 가격은 사용자가 변조할 수 있고, 메뉴나 옵션 가격이 장바구니에 담긴 뒤 변경될 수도 있기 때문입니다. 서버는 menuId와 option ID를 기준으로 현재 판매 상태와 가격을 조회해 기본가·옵션 추가금·수량을 다시 계산해야 합니다. 클라이언트가 보낸 `totalAmount`는 화면 표시용 참고값으로만 보고 결제 금액의 기준으로 신뢰하지 않겠습니다. 계산 결과가 클라이언트 예상값과 다르면 가격 변경 안내나 재확인을 반환하겠습니다.”

## 문제 6. 결제 성공 후 주문 저장이 실패하면 어떻게 처리하나요?

**답변:** “외부 결제 승인과 DB 저장은 서로 다른 시스템이므로 하나의 DB 롤백으로 모두 되돌릴 수 없습니다. 먼저 결제 승인 ID와 주문 식별자를 기준으로 현재 상태를 조회하고, 주문 저장을 안전하게 재처리할 수 있는지 확인합니다. 재처리가 불가능하거나 주문 생성이 취소되면 결제 취소·환불 같은 보상 작업을 수행하고, 사용자에게는 결제는 되었지만 주문 확인이 필요한 상태를 명확히 안내합니다. 이 흐름은 운영자가 재처리할 수 있도록 상태와 로그를 남겨야 합니다.”

## 문제 7. 사용자가 결제 버튼을 연속으로 누르면 어떻게 하나요?

**답변:** “프론트에서는 첫 클릭 이후 버튼을 비활성화하고 로딩 상태를 표시하겠습니다. 하지만 브라우저 중복 탭이나 네트워크 재시도는 프론트에서 완전히 막을 수 없으므로, 결제 요청에 UUID 형태의 idempotency key를 포함합니다. 서버는 같은 주문과 키가 이미 처리되었으면 새 결제를 만들지 않고 기존 결과를 반환하며, 이미 승인된 주문에는 `PAYMENT_ALREADY_APPROVED`처럼 명확한 상태를 반환합니다. 테스트에서는 빠른 연속 클릭뿐 아니라 타임아웃 후 재시도도 확인하겠습니다.”

## 문제 8. 품절 메뉴가 장바구니에 들어 있으면 어떻게 처리하나요?

**답변:** “장바구니를 보여줄 때 품절 상태를 표시하는 것은 사용자 경험을 위한 것이고, 최종 방어는 주문 생성 API에서 해야 합니다. 서버는 메뉴 자체의 품절, 옵션 항목 품절, 특정 재료 품절로 인해 주문 불가능한지를 다시 계산합니다. 문제가 있으면 주문을 저장하지 않고 어떤 항목이 문제인지 오류 코드와 함께 반환합니다. 프론트는 장바구니를 유지한 채 해당 상품 삭제나 옵션 수정으로 이동할 수 있게 하겠습니다.”

## 문제 9. 관리자 주문 상태 변경과 TTS는 어떻게 연결하나요?

**답변:** “관리자 화면에서 상태 변경을 누르면 `PATCH /api/admin/orders/{orderId}/{status}`를 호출하고, 서버가 허용된 상태 전이인지 확인합니다. MVP 기준으로는 `RECEIVED → PREPARING → COMPLETED` 순서가 안전하며, 바로 `RECEIVED → COMPLETED`를 허용할지는 운영 정책으로 명확히 해야 합니다. 프론트는 PATCH 성공 응답을 받은 뒤에만 UI를 갱신하고 TTS를 실행합니다. 완료 요청이 중복되면 서버는 현재 상태를 반환하고 새 완료 이벤트나 TTS가 다시 생성되지 않도록 하겠습니다.”

## 문제 10. 관리자 주문 polling에서 주의할 점은 무엇인가요?

**답변:** “이전 요청이 끝나기 전에 다음 요청을 보내면 응답 순서가 뒤집혀 최신 상태가 오래된 상태로 덮어써질 수 있습니다. 따라서 진행 중 요청이 있으면 새 요청을 만들지 않고, 컴포넌트가 사라질 때 interval과 요청 정리를 수행하겠습니다. 오류가 발생해도 기존 주문 카드를 빈 목록으로 지우지 않고 마지막 정상 데이터를 유지하며 Toast를 표시합니다. 다른 관리자가 먼저 상태를 바꾼 경우에는 서버의 현재 상태를 기준으로 다시 동기화해야 합니다.”

## 문제 11. 실제 구현 범위와 설계 범위를 어떻게 구분해 말하겠습니까?

**답변:** “확인된 사실과 계획을 분리해서 말하겠습니다. 확인된 흐름은 Admin 주문 화면의 `useOrdersQuery → ordersApi → GET /api/admin/orders → Controller → Service → Mapper → DB`입니다. 반면 확인 시점의 키오스크 메뉴 화면은 `kiosk.json` 목업을 직접 사용한 부분이 있었고, 키오스크 주문·결제는 API 계약과 골격을 함께 확인한 상태였습니다. 따라서 ‘전체 결제를 완성했다’고 표현하지 않고, 실제로 확인·구현한 화면과 다음 정합성 작업을 구분해 설명하겠습니다.”

## 문제 12. 프로젝트에서 가장 어려웠던 문제를 어떻게 해결했나요?

**답변:** “먼저 현상을 재현하고 기대 결과와 실제 결과를 분리했습니다. 브라우저 Network에서 URL·메서드·요청 본문·상태 코드를 확인한 뒤, 서버 로그에서 Controller 진입 여부를 보고, Service의 검증과 Mapper SQL, 마지막으로 DB의 실제 행을 확인했습니다. 원인을 찾은 뒤에는 관련 없는 리팩터링을 하지 않고 계약에 필요한 최소 수정만 적용했으며, 빌드 성공만으로 끝내지 않고 API 응답과 DB 상태, 화면 동작을 각각 확인했습니다.”

## 문제 13. 개발 시간이 더 주어진다면 무엇을 개선하겠습니까?

**답변:** “첫째 키오스크 API 경로와 Vite 프록시, 응답 envelope, DTO 필드의 정합성을 하나의 계약 테스트로 고정하겠습니다. 둘째 주문·결제의 중복 요청과 승인 후 저장 실패를 통합 테스트로 검증하겠습니다. 셋째 로그·메트릭·추적 ID를 추가해 오류가 발생했을 때 한 주문의 요청 흐름을 따라갈 수 있게 하겠습니다. 마지막으로 접근성, 품절, 네트워크 단절, 자동 초기화 같은 키오스크 특화 시나리오를 회귀 테스트 목록으로 관리하겠습니다.”

---

# SECTION 08 · 트렌디한 추가 꼬리 질문

## 문제 1. 요즘 프론트엔드에서 `useEffect`를 무조건 많이 쓰지 않는 이유는 무엇인가요?

**답변:** Effect는 렌더링 결과를 외부 시스템과 동기화하는 도구이지, 모든 상태 변경을 처리하는 만능 이벤트 핸들러가 아니기 때문입니다. 단순히 props를 가공하거나 화면에 표시할 값을 계산하는 데 Effect를 사용하면 불필요한 렌더링과 상태 중복이 생길 수 있습니다. 계산은 렌더링 과정이나 메모이제이션으로 처리하고, 사용자 클릭으로 발생한 작업은 이벤트 핸들러에서 처리하며, API·타이머·이벤트 리스너처럼 외부 시스템과 연결할 때 Effect를 사용하겠습니다.

## 문제 2. 테스트 피라미드를 설명하세요.

**답변:** 가장 아래에는 빠르고 많은 단위 테스트가 있고, 그 위에 여러 모듈의 연결을 확인하는 통합 테스트, 가장 위에 실제 사용자 흐름을 확인하는 E2E 테스트가 있습니다. 주문 가격 계산이나 상태 전이 같은 순수 업무 규칙은 단위 테스트로 많이 검증하고, Controller와 DB 매핑은 통합 테스트로 확인하며, 메뉴 선택부터 결제 완료까지는 핵심 E2E 시나리오로 검증하겠습니다. 테스트 수만 늘리기보다 실패했을 때 원인을 빨리 찾을 수 있도록 계층별 책임을 나누는 것이 중요합니다.

## 문제 3. 관측 가능성이란 무엇인가요?

**답변:** 관측 가능성은 시스템 내부를 직접 보지 않고도 외부 출력으로 현재 상태와 원인을 추론할 수 있는 능력입니다. 로그는 사건의 상세 내용, 메트릭은 응답 시간·오류율·처리량 같은 추세, 트레이스는 하나의 요청이 여러 계층을 통과한 흐름을 보여줍니다. ASAK에서는 주문번호나 추적 ID를 기준으로 프론트 요청, 주문 생성, 결제, 관리자 상태 변경을 연결해 보면 장애 분석이 쉬워집니다. 개인정보와 결제 민감정보는 반드시 마스킹하겠습니다.

## 문제 4. 최신 기술을 도입할 때 어떤 기준을 사용하나요?

**답변:** 유행보다 문제 적합성, 팀의 이해도, 운영 비용, 보안, 기존 코드와의 호환성, 롤백 가능성을 먼저 봅니다. 작은 프로토타입이나 한 기능에서 효과를 검증하고, 도입하지 않았을 때의 비용과 비교한 뒤 결정하겠습니다. 기술을 선택한 이유와 제한사항을 문서화하면 나중에 팀원이 같은 판단을 반복하지 않아도 됩니다.

## 문제 5. 개발자로서 AI 시대에 필요한 역량은 무엇이라고 생각하나요?

**답변:** 코드를 빨리 생성하는 능력보다 문제를 정확히 정의하고 결과를 검증하는 능력이 더 중요하다고 생각합니다. 요구사항을 데이터 흐름과 테스트 조건으로 바꾸고, 보안·성능·운영 영향을 판단할 수 있어야 AI의 잘못된 답변도 걸러낼 수 있습니다. 저는 AI를 보조 도구로 활용하되 실제 코드, 공식 문서, 실행 결과를 근거로 최종 판단하겠습니다.

---

# 면접 답변 마무리 문장

“저는 기능이 화면에서 보이는 것만으로 끝내지 않고, 프론트 요청부터 Controller·Service·Mapper·DB, 그리고 실패·재시도·운영 로그까지 하나의 흐름으로 확인하려고 합니다. 최신 기술을 무조건 도입하기보다 프로젝트 문제에 적합한지, 팀이 유지보수할 수 있는지, 테스트와 롤백이 가능한지를 기준으로 판단하겠습니다. AI를 활용하더라도 제가 이해하고 검증할 수 있는 코드만 사용하겠습니다.”

## 참고 기준

- React 공식 문서: Effect는 외부 시스템과 동기화할 때 사용하고 불필요한 Effect는 줄이는 방향을 권장합니다.
- Spring Framework 공식 문서: Controller 입력에 대한 Bean Validation과 전역 Validator 구성을 지원합니다.
- OWASP API Security Top 10: 객체 수준 인가, 민감한 업무 흐름의 무제한 접근, 과도한 데이터 노출 등을 API 보안 위험으로 다룹니다.
