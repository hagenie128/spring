# Practice B — React JWT + API (기초)

> **CRUD 전체 연습**은 [quest/19-react-jwt-practice](../../19-react-jwt-practice/) Step 1~10 을 따르세요.  
> 이 문서는 quest/18 안에서 빠르게 읽기(R)만 할 때 사용합니다.

> 백엔드 API가 `/api/students`든 `/api/books`든 **구조는 동일**합니다.

**선행:** [00-프로젝트-만들기.md](00-프로젝트-만들기.md)  
**백엔드:** `step17-board-backend` 실행 (또는 시험에 제공되는 API)

---

## 문제 1 — Axios 공통 설정 (20분)

### TODO

| # | 파일 | 할 일 |
|---|------|-------|
| 1 | `axiosInstance.js` | `baseURL: process.env.REACT_APP_API_URL` |
| 2 | `axiosInstance.js` | 요청 인터셉터 — `Authorization: Bearer ${token}` |

**확인:** Network 탭에서 요청 URL이 `localhost:8888` 인지

---

## 문제 2 — 로그인 + AuthContext (60분)

### TODO

| # | 파일 | 할 일 |
|---|------|-------|
| 1 | `authApi.js` | `login({ username, password })` → POST `/auth/login` |
| 2 | `authApi.js` | `me()` → GET `/auth/me` |
| 3 | `AuthContext.jsx` | login → token localStorage → me → setUser |
| 4 | `index.js` | `AuthProvider`로 App 감싸기 |
| 5 | `LoginPage.jsx` | ref면 `.current.value`, state면 변수 그대로 |

### 로그인 Body (시험에 적혀 있을 패턴)

```json
{ "username": "...", "password": "..." }
```

### 응답에서 꺼낼 키 (문제지 확인!)

```json
{ "accessToken": "...", "refreshToken": "...", "tokenType": "Bearer" }
```

**완료:** 로그인 후 localStorage에 `accessToken`

---

## 문제 3 — 목록 API 읽기 (60분)

### TODO (게시판 API 기준)

| # | 파일 | 할 일 |
|---|------|-------|
| 1 | `itemApi.js` | `getList()` → GET `/api/posts?page=1&size=20` |
| 2 | `ItemListPage.jsx` | `useEffect` + `useState([])` |
| 3 | `ItemListPage.jsx` | `response.data.list` 를 map으로 테이블 출력 |
| 4 | `App.js` | `/` → ItemListPage |

### API가 바뀌면 확인할 것 (문제지 1분)

| 확인 | 예시 (게시판) | 예시 (다른 API) |
|------|---------------|-----------------|
| URL | `/api/posts` | `/api/students` |
| 목록 키 | `list` | `data`, `items`, `students` |
| PK 필드 | `bno` | `id`, `studentId` |
| 제목 필드 | `title` | `name` |

**완료:** 로그인 없이 목록이 화면에 표시 (공개 API인 경우)

---

## 문제 4 — 상세 (시간 남으면, 30분)

- `useParams()` 로 id 받기
- `GET /api/posts/{id}` 호출
- 라우트 `:id` 와 `useParams` 이름 **일치** 필수

---

## 2일 3시간 플랜에서는 여기까지

수정·삭제·댓글·CSS는 **시험 범위에 나오면** 그때 패턴만 추가.

📄 전체 일정: [../2일-3시간-플랜.md](../2일-3시간-플랜.md)
