import axiosInstance from "./axiosInstance";

export const postApi = {
  // TODO: GET /api/posts — params: page, keyword, size
  getPage: (page, keyword, size) => {
    // return axiosInstance.get(...)
  },
};
