# 키오스크 면접 예상 질문지 — 문제·답변

> 질문 바로 아래에 답변을 배치했습니다. `[ASAK 프로젝트]` 섹션은 실제 프로젝트 기준 답변입니다.

## 목차

1. Java
2. 웹·프론트엔드
3. HTTP와 REST API
4. 데이터베이스
5. Spring Boot·백엔드
6. Git·협업·AI
7. ASAK 프로젝트 경험
8. 마지막 한마디

---

# SECTION 01 · Java

### 문제 1. 클래스, 객체, 인스턴스의 차이를 설명하세요.

**답변:** 클래스는 객체를 만들기 위한 설계도이고, 객체는 그 설계도로 만든 실체입니다. 인스턴스는 특정 클래스에서 생성된 객체를 의미합니다.

### 문제 2. 캡슐화, 상속, 다형성, 추상화를 설명하세요.

**답변:** 캡슐화는 데이터와 기능을 묶고 접근을 제한하는 것이며, 상속은 기존 기능을 확장하는 것입니다. 다형성은 같은 타입으로 여러 구현을 다루는 것이고, 추상화는 복잡한 구현을 숨기고 핵심 기능만 표현하는 것입니다.

### 문제 3. 오버로딩과 오버라이딩의 차이는 무엇인가요?

**답변:** 오버로딩은 같은 메서드 이름을 매개변수만 다르게 여러 개 정의하는 것이고, 오버라이딩은 자식 클래스가 부모 클래스의 메서드 동작을 재정의하는 것입니다.

### 문제 4. 추상 클래스와 인터페이스의 차이는 무엇인가요?

**답변:** 추상 클래스는 공통 상태와 구현을 공유할 때 적합하고, 인터페이스는 구현체가 지켜야 할 계약을 정의할 때 적합합니다. 클래스는 하나만 상속할 수 있지만 여러 인터페이스를 구현할 수 있습니다.

### 문제 5. 접근 제한자의 차이를 설명하세요.

**답변:** public은 어디서나 접근할 수 있고, protected는 같은 패키지와 상속 관계에서 접근할 수 있습니다. default는 같은 패키지에서만, private은 선언된 클래스 내부에서만 접근할 수 있습니다.

### 문제 6. `==`와 `equals()`의 차이는 무엇인가요?

**답변:** 기본형에서 `==`는 값을 비교하고 참조형에서는 참조를 비교합니다. `equals()`는 객체의 논리적 동등성을 비교하도록 재정의할 수 있습니다.

### 문제 7. String, StringBuilder, StringBuffer의 차이는 무엇인가요?

**답변:** String은 불변 객체입니다. StringBuilder는 단일 스레드에서 문자열을 자주 변경할 때 빠르고, StringBuffer는 동기화가 필요한 환경에서 사용할 수 있지만 상대적으로 느립니다.

### 문제 8. List, Set, Map의 차이는 무엇인가요?

**답변:** List는 순서와 중복을 허용하고, Set은 중복을 허용하지 않습니다. Map은 키와 값의 쌍으로 저장하며 키는 고유해야 합니다.

### 문제 9. ArrayList와 LinkedList의 차이는 무엇인가요?

**답변:** ArrayList는 인덱스 조회가 빠르고 중간 삽입·삭제가 불리합니다. LinkedList는 연결 구조라 삽입·삭제에 유리할 수 있지만 조회가 느립니다.

### 문제 10. 체크 예외와 언체크 예외의 차이는 무엇인가요?

**답변:** 체크 예외는 컴파일 시 처리를 요구하고, 언체크 예외는 실행 중 발생합니다. 예외는 숨기기보다 원인을 기록하고 사용자에게는 안전한 오류 응답을 반환해야 합니다.

### 문제 11. Stream을 사용하는 이유와 장단점은 무엇인가요?

**답변:** 필터링·변환·정렬·집계를 선언적으로 표현할 수 있어 가독성과 조합성이 좋습니다. 다만 디버깅과 성능 분석이 어려울 수 있어 단순한 로직에는 일반 반복문이 더 적합할 수 있습니다.

### 문제 12. 의존성이 높은 코드의 문제와 개선 방법은 무엇인가요?

**답변:** 한 클래스의 변경이 다른 클래스에 연쇄적으로 영향을 주고 테스트가 어려워집니다. 인터페이스와 생성자 주입을 사용해 의존성을 명시하고 책임을 분리하겠습니다.

### 문제 13. 가격이 10,000원 이상인 상품을 가격순으로 정렬하려면 어떻게 하겠습니까?

**답변:** `filter`로 가격 조건을 적용한 뒤 `sorted`로 정렬하고 결과를 리스트로 변환합니다. null 가격, 오름차순 여부, 결과가 없을 때의 동작도 함께 정의하겠습니다.

### 문제 14. NullPointerException은 어떻게 찾겠습니까?

**답변:** 스택 트레이스의 발생 위치를 확인하고 해당 변수의 생성·주입·조회 흐름을 따라갑니다. 입력값과 DB 조회 결과를 확인한 뒤 재현 테스트를 만들고 최소 범위만 수정합니다.

---

# SECTION 02 · 웹·프론트엔드

### 문제 1. HTML, CSS, JavaScript의 역할은 무엇인가요?

**답변:** HTML은 구조, CSS는 표현과 배치, JavaScript는 사용자 동작과 상태 처리를 담당합니다.

### 문제 2. 반응형 웹이란 무엇인가요?

**답변:** 화면 크기와 기기에 따라 레이아웃과 콘텐츠가 적절히 변하는 방식입니다. 유연한 영역, 미디어 쿼리, 터치 영역과 접근성을 고려합니다.

### 문제 3. 웹 표준과 접근성이 필요한 이유는 무엇인가요?

**답변:** 웹 표준은 브라우저 호환성과 유지보수성을 높이고, 접근성은 장애인과 다양한 기기의 사용자도 서비스를 이용할 수 있게 합니다.

### 문제 4. Flex와 Grid의 차이는 무엇인가요?

**답변:** Flex는 한 방향 정렬에 강하고 Grid는 행과 열을 함께 관리하는 2차원 레이아웃에 적합합니다.

### 문제 5. DOM이란 무엇인가요?

**답변:** 브라우저가 HTML 문서를 객체 트리로 표현한 것입니다. JavaScript는 DOM을 조회하거나 수정할 수 있습니다.

### 문제 6. 동기와 비동기의 차이는 무엇인가요?

**답변:** 동기는 앞 작업이 끝난 뒤 다음 작업을 하고, 비동기는 오래 걸리는 작업을 맡긴 뒤 다른 작업을 진행합니다. 비동기는 응답성이 좋지만 로딩·성공·실패 상태 관리가 필요합니다.

### 문제 7. SPA의 특징은 무엇인가요?

**답변:** 최초에 애플리케이션을 로드하고 이후 라우팅과 화면 변경을 클라이언트가 처리합니다. 화면 전환이 빠르지만 초기 로딩과 상태 관리가 중요합니다.

### 문제 8. React의 props와 state 차이는 무엇인가요?

**답변:** props는 부모가 자식에게 전달하는 읽기 전용 입력이고, state는 컴포넌트 내부에서 변경되며 렌더링을 유발하는 값입니다.

### 문제 9. React 컴포넌트를 분리하는 기준은 무엇인가요?

**답변:** 반복되는 UI, 독립적인 책임, 별도 상태나 이벤트 흐름이 있는 부분을 기준으로 분리합니다. 지나치게 잘게 나누어 흐름이 어려워지지 않게 합니다.

### 문제 10. `useEffect`는 언제 사용하나요?

**답변:** API 조회, 이벤트 등록, 타이머처럼 렌더링 외부 시스템과 동기화할 때 사용합니다. 단순한 값 계산에는 사용하지 않습니다.

### 문제 11. CORS 오류는 왜 발생하나요?

**답변:** 브라우저가 다른 출처의 요청을 제한하기 때문에 발생합니다. 서버가 허용할 출처와 메서드·헤더를 응답에 설정해야 합니다.

---

# SECTION 03 · HTTP와 REST API

### 문제 1. HTTP 요청과 응답의 구조는 무엇인가요?

**답변:** 요청은 메서드·URL·헤더·본문으로 구성되고, 응답은 상태 코드·헤더·본문으로 구성됩니다.

### 문제 2. GET, POST, PUT, PATCH, DELETE의 용도는 무엇인가요?

**답변:** GET은 조회, POST는 생성, PUT은 전체 수정, PATCH는 부분 수정, DELETE는 삭제에 사용합니다.

### 문제 3. Path Variable과 Query Parameter의 차이는 무엇인가요?

**답변:** Path Variable은 `/orders/{orderId}`처럼 특정 리소스를 식별하고, Query Parameter는 검색·필터·정렬·페이지 조건을 전달합니다.

### 문제 4. RESTful API란 무엇인가요?

**답변:** 자원을 URI로 표현하고 HTTP 메서드와 상태 코드로 동작을 표현하는 API입니다. ASAK은 공통적으로 `{ success, status, code, message, data }` 응답 형식을 사용하도록 계약했습니다.

### 문제 5. 주요 HTTP 상태 코드를 설명하세요.

**답변:** 200은 성공, 201은 생성 성공, 204는 본문 없는 성공입니다. 400은 잘못된 요청, 401은 인증 실패, 403은 권한 부족, 404는 리소스 없음, 409는 충돌, 500은 서버 오류입니다.

### 문제 6. API 명세에는 무엇이 들어가야 하나요?

**답변:** URL, HTTP 메서드, 인증 방식, 요청·응답 필드, 타입과 필수 여부, 상태 코드, 오류 코드, 예시를 포함해야 합니다.

### 문제 7. 주문 요청이 두 번 들어오면 어떻게 처리하겠습니까?

**답변:** 프론트 버튼을 비활성화하되 그것만 믿지 않고, 서버에서 idempotency key와 유니크 조건을 검사합니다. 이미 처리된 요청이면 기존 결과를 반환해 중복 주문과 결제를 막겠습니다.

---

# SECTION 04 · 데이터베이스

### 문제 1. 기본키와 외래키를 설명하세요.

**답변:** 기본키는 테이블의 행을 고유하게 식별하고, 외래키는 다른 테이블의 기본키를 참조해 관계를 표현합니다.

### 문제 2. 정규화가 필요한 이유는 무엇인가요?

**답변:** 데이터 중복과 삽입·수정·삭제 이상을 줄이기 위해 필요합니다. 다만 조회 성능과 구조 복잡성을 고려해 필요한 경우에만 반정규화합니다.

### 문제 3. 1:N과 N:M 관계를 설명하세요.

**답변:** 주문 하나에 여러 주문 상세가 연결되는 것이 1:N입니다. 상품과 주문처럼 양쪽이 여러 개 연결되는 N:M은 중간 테이블로 표현합니다.

### 문제 4. INNER JOIN과 LEFT JOIN의 차이는 무엇인가요?

**답변:** INNER JOIN은 양쪽에 모두 존재하는 행만 반환하고, LEFT JOIN은 왼쪽 테이블의 모든 행을 유지하며 오른쪽 데이터가 없으면 NULL을 반환합니다.

### 문제 5. 인덱스를 많이 만들면 어떤 문제가 있나요?

**답변:** 조회는 빨라질 수 있지만 INSERT·UPDATE·DELETE 때 인덱스도 갱신해야 하므로 쓰기 성능과 저장 공간이 나빠집니다. 실행 계획으로 효과를 확인해야 합니다.

### 문제 6. 트랜잭션과 ACID를 설명하세요.

**답변:** 트랜잭션은 여러 DB 작업을 하나의 논리적 작업 단위로 묶는 것입니다. ACID는 원자성·일관성·격리성·지속성입니다.

### 문제 7. SQL Injection을 어떻게 방지하나요?

**답변:** 사용자 입력이 SQL 구조에 들어가지 않도록 Prepared Statement와 MyBatis `#{}`를 사용하고, 입력 검증과 DB 최소 권한도 적용합니다.

### 문제 8. 주문과 주문 상세를 분리하는 이유는 무엇인가요?

**답변:** 한 주문에 여러 상품을 저장해야 하므로 기본 정보와 반복되는 상품 정보를 분리합니다. 상세에는 상품·수량·주문 당시 가격을 저장해 주문 내용을 보존합니다.

---

# SECTION 05 · Spring Boot·백엔드

### 문제 1. Controller, Service, Repository 또는 Mapper의 역할은 무엇인가요?

**답변:** Controller는 HTTP 입출력, Service는 업무 규칙과 트랜잭션, Repository·Mapper는 DB 접근을 담당합니다.

### 문제 2. DTO, Entity, VO의 차이는 무엇인가요?

**답변:** DTO는 계층 간 전송용이고 Entity는 DB 매핑용입니다. VO는 값 자체의 의미와 동등성을 표현합니다. 외부 요청을 Entity에 직접 받지 않는 것이 안전합니다.

### 문제 3. IoC와 DI란 무엇인가요?

**답변:** IoC는 객체 생성과 생명주기 제어를 프레임워크가 맡는 것이고, DI는 필요한 의존성을 외부에서 주입받는 것입니다.

### 문제 4. 생성자 주입을 사용하는 이유는 무엇인가요?

**답변:** 필수 의존성을 명확히 하고 불변 필드로 만들 수 있습니다. 객체 생성 시 누락을 확인할 수 있고 테스트에서 대체 구현을 주입하기도 쉽습니다.

### 문제 5. `@Controller`와 `@RestController`의 차이는 무엇인가요?

**답변:** `@Controller`는 주로 View 이름을 반환하고, `@RestController`는 반환값을 HTTP 응답 본문으로 직접 반환합니다.

### 문제 6. `@RequestParam`, `@PathVariable`, `@RequestBody`의 차이는 무엇인가요?

**답변:** 각각 쿼리 파라미터, URL 경로 값, JSON 같은 요청 본문을 받는 데 사용합니다.

### 문제 7. `@Transactional`의 역할은 무엇인가요?

**답변:** 여러 DB 작업을 하나의 트랜잭션으로 묶어 성공하면 커밋하고 실패하면 롤백합니다. 외부 결제 시스템은 별도이므로 실패 시 보상 처리도 설계해야 합니다.

### 문제 8. MyBatis의 `#{}`와 `${}` 차이는 무엇인가요?

**답변:** `#{}`는 값을 안전하게 바인딩하고 `${}`는 문자열을 그대로 치환합니다. `${}`는 SQL Injection 위험이 있어 사용자 입력에 사용하지 않습니다.

### 문제 9. 전역 예외 처리는 왜 필요한가요?

**답변:** 오류 응답 형식을 통일하고 Controller의 중복 코드를 줄이기 위해 필요합니다. `@RestControllerAdvice`와 `@ExceptionHandler`로 구현할 수 있습니다.

### 문제 10. 인증과 인가의 차이는 무엇인가요?

**답변:** 인증은 사용자가 누구인지 확인하는 것이고, 인가는 인증된 사용자가 어떤 기능을 사용할 수 있는지 확인하는 것입니다.

---

# SECTION 06 · Git·협업·AI

### 문제 1. Git과 GitHub의 차이는 무엇인가요?

**답변:** Git은 로컬 버전 관리 시스템이고 GitHub는 Git 저장소를 호스팅하며 이슈·리뷰·협업 기능을 제공하는 서비스입니다.

### 문제 2. 브랜치를 사용하는 이유는 무엇인가요?

**답변:** 기능별 작업을 격리하고 코드 리뷰와 충돌 해결을 쉽게 하기 위해 사용합니다.

### 문제 3. API 형식이 팀원마다 다르면 어떻게 해결하겠습니까?

**답변:** 프론트 코드만 임의로 바꾸지 않고 실제 Controller·DTO·응답과 명세를 대조합니다. URL, 메서드, 필드명, 타입, 상태 코드, 오류 형식을 합의하고 테스트 기준을 남깁니다.

### 문제 4. 생성형 AI를 어떻게 사용했나요?

**답변:** 코드 설명, 오류 원인 후보 정리, 테스트 항목과 문서 초안처럼 검토 가능한 보조 작업에 사용했습니다. 실제 코드·공식 문서·실행 결과를 확인하고 이해하지 못한 코드는 사용하지 않았습니다.

### 문제 5. AI가 작성한 코드가 맞는지 어떻게 검증하나요?

**답변:** 프로젝트 버전과 의존성을 확인하고, 공식 문서·IDE·최소 재현 코드·자동 테스트로 검증합니다. 비밀번호·토큰·개인정보는 AI에 전달하지 않습니다.

---

# SECTION 07 · ASAK 프로젝트 경험

### 문제 1. 수행한 프로젝트를 1분 이내로 설명해 주세요.

**답변:** “ASAK은 고객용 키오스크와 관리자 운영 화면을 연결한 주문 서비스입니다. 고객은 매장 식사 또는 포장을 선택하고 메뉴·옵션·제외 재료를 고른 뒤 장바구니와 결제를 진행합니다. 관리자는 주문을 접수·조리·완료 상태로 관리하고 실시간 주문 보드와 TTS 안내를 사용합니다. 프론트·백엔드 REST API와 관계형 DB를 기준으로 메뉴, 주문, 주문 상세, 결제 데이터를 연결했습니다.”

### 문제 2. 프로젝트의 전체 요청 처리 흐름을 설명해 주세요.

**답변:** “React 화면에서 사용자가 행동하면 API 요청이 발생하고, Spring Controller가 DTO를 받습니다. Service가 검증과 업무 규칙·트랜잭션을 처리한 뒤 Repository 또는 Mapper가 DB를 조회·변경합니다. 응답은 공통 envelope로 프론트에 돌아오고, 프론트는 성공·로딩·빈 상태·오류 상태를 화면에 반영합니다.”

### 문제 3. ASAK의 주문 흐름을 설명해 주세요.

**답변:** “Home에서 `orderType`을 선택하고 Menu List, Menu Detail, Cart, 주문 생성, Payment, Complete 순서로 이동합니다. 장바구니 검증 후 `POST /api/kiosk/orders`를 호출하고 서버가 수량·메뉴·옵션·품절·가격을 다시 검증합니다. 주문 생성 응답의 `orderId`, `orderNo`, `totalAmount`를 사용해 결제 단계로 이동합니다.”

### 문제 4. 주문·결제·주문 상세 데이터의 관계는 무엇인가요?

**답변:** “주문은 기본 정보, 주문 상세는 주문에 포함된 상품별 수량과 당시 가격입니다. 주문 하나에 여러 상세가 있으므로 1:N 관계입니다. 결제는 주문과 연결되지만 승인 상태와 승인 금액을 별도로 관리해 주문 완료와 결제 승인을 혼동하지 않습니다.”

### 문제 5. 결제 성공 후 주문 저장에 실패하면 어떻게 처리하나요?

**답변:** “외부 결제 승인과 DB 저장은 같은 트랜잭션이라고 가정하지 않습니다. 주문 상태를 재조회하고 저장 재처리 또는 결제 취소 보상 절차를 수행합니다. 사용자에게는 최종 주문 상태가 확정되기 전 성공으로 오인할 메시지를 보여주지 않습니다.”

### 문제 6. 결제 버튼 연속 클릭은 어떻게 막나요?

**답변:** “프론트에서 처리 중 버튼을 비활성화하고, 서버에서는 `idempotencyKey`와 이미 승인된 결제인지 확인합니다. 중복 요청은 기존 결과를 반환하거나 `PAYMENT_IN_PROGRESS`, `PAYMENT_ALREADY_APPROVED`로 처리합니다.”

### 문제 7. 품절된 메뉴가 장바구니에 있으면 어떻게 하나요?

**답변:** “장바구니 표시 시점뿐 아니라 주문 생성 직전에 서버가 메뉴·옵션·재료의 품절 여부를 다시 확인합니다. 품절이면 주문을 생성하지 않고 해당 항목을 표시해 삭제하거나 옵션을 수정하도록 안내합니다.”

### 문제 8. 관리자 주문 상태 변경 흐름을 설명해 주세요.

**답변:** “관리자 화면에서 주문 카드의 상태 변경을 요청하면 `PATCH /api/admin/orders/{orderId}/{status}`가 호출됩니다. 백엔드가 상태 전이를 검증하고 성공하면 UI를 갱신합니다. `COMPLETED`일 때는 PATCH 성공 후에만 TTS를 실행하며, 중복 완료로 TTS가 반복되지 않게 합니다.”

### 문제 9. 가장 어려웠던 오류는 어떻게 해결했나요?

**답변:** “먼저 재현 조건을 고정하고 브라우저 요청, 서버 로그, DB 쿼리 순서로 범위를 좁혔습니다. 요청이 서버에 도착했는지, Controller·Service가 실행됐는지, Mapper와 실제 DB 결과가 일치하는지 확인한 뒤 수정하고 API와 브라우저 흐름으로 다시 검증했습니다.”

### 문제 10. ASAK에서 실제 확인 범위와 미완성 범위를 어떻게 설명하겠습니까?

**답변:** “관리자 주문 목록은 `useOrdersQuery → ordersApi → GET /api/admin/orders → Controller → Service → Mapper → DB` 흐름을 확인했습니다. 반면 키오스크 메뉴는 확인 시점에 목업 사용 부분이 있었고, 주문·결제는 API 골격과 계약을 함께 확인한 상태였습니다. 따라서 완성된 전체 연동이라고 과장하지 않고 확인된 부분과 다음 구현 범위를 구분해 말하겠습니다.”

### 문제 11. 개발 시간이 더 주어진다면 무엇을 개선하겠습니까?

**답변:** “키오스크 API 경로와 프록시, 응답 필드의 정합성을 맞추고 주문·결제 통합 테스트를 강화하겠습니다. 이후 중복 요청, 가격 변경, 품절, 네트워크 실패, 결제 승인 후 저장 실패를 자동화된 시나리오로 검증하겠습니다.”

### 문제 12. 프로젝트의 보안상 부족한 부분은 무엇인가요?

**답변:** “인증·인가, HTTPS, 입력 검증, 민감정보 로그 차단, 결제 위변조 방지, 중복 요청 방어를 계속 점검해야 합니다. 특히 키오스크가 DB에 직접 접근하지 않고 서버 API를 통해서만 검증되도록 해야 합니다.”

### 문제 13. 테스트는 어떤 방식으로 진행했나요?

**답변:** “정상 흐름뿐 아니라 빈 목록, 잘못된 입력, 품절, 가격 변경, 중복 클릭, 네트워크 오류, 권한 오류를 확인합니다. API 응답과 DB 상태를 따로 검증하고, 브라우저에서는 로딩·빈 상태·오류 화면과 화면 전환까지 확인합니다.”

### 문제 14. 본인이 작성한 코드 중 가장 자신 있는 부분은 무엇인가요?

**답변:** “[본인이 실제 담당한 파일과 기능]을 기준으로 말씀드리겠습니다. 입력값이 어디서 들어와 어떤 상태와 API 필드로 변환되는지, 실패 시 어떤 화면을 보여주는지, 어떤 테스트로 확인했는지까지 설명하겠습니다. 직접 작성하지 않은 부분은 팀 코드라고 구분하겠습니다.”

---

# SECTION 08 · 마지막 한마디

### 문제. 개발자로서 중요하게 생각하는 점은 무엇인가요?

**답변:** “기능이 화면에서 보이는 것만으로 끝내지 않고 프론트 요청부터 Controller·Service·Mapper·DB, 그리고 오류와 재시도까지 데이터 흐름으로 확인하는 것을 중요하게 생각합니다. AI를 사용하더라도 실제 코드와 공식 문서, 테스트 결과를 기준으로 검증하고 제가 이해하지 못한 코드는 사용하지 않겠습니다.”
