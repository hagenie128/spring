# 키오스크 면접 예상 질문지

## 1. Java

1. 클래스, 객체, 인스턴스의 차이를 설명하세요.
2. 객체지향 프로그래밍의 특징인 캡슐화, 상속, 다형성, 추상화를 설명하세요.
3. 오버로딩과 오버라이딩의 차이를 설명하세요.
4. 추상 클래스와 인터페이스의 차이를 설명하세요.
5. 접근 제한자 `public`, `protected`, `default`, `private`의 차이를 설명하세요.
6. `==` 연산자와 `equals()` 메서드의 차이를 설명하세요.
7. `String`, `StringBuilder`, `StringBuffer`의 차이를 설명하세요.
8. `List`, `Set`, `Map`의 특징과 차이를 설명하세요.
9. `ArrayList`와 `LinkedList`의 차이를 설명하세요.
10. Java에서 예외 처리란 무엇이며, 체크 예외와 언체크 예외의 차이는 무엇인가요?
11. `try-catch-finally`의 역할을 설명하세요.
12. 스레드와 프로세스의 차이를 설명하세요.
13. 멀티스레드 환경에서 발생할 수 있는 문제에는 무엇이 있나요?
14. Java Stream을 사용하는 이유와 장단점을 설명하세요.
15. 의존성이 높은 코드에서 발생할 수 있는 문제를 설명하세요.

### 상황형 질문

1. 여러 상품 중 가격이 10,000원 이상인 상품만 찾아 가격순으로 정렬하려면 어떻게 구현하겠습니까?
2. `NullPointerException`이 발생했을 때 어떤 순서로 원인을 찾겠습니까?
3. 동일한 코드가 여러 클래스에서 반복된다면 어떻게 개선하겠습니까?

---

## 2. 웹·프론트엔드 기초

1. HTML, CSS, JavaScript가 각각 담당하는 역할을 설명하세요.
2. 반응형 웹이 무엇인지 설명하세요.
3. 웹 표준과 웹 접근성을 지켜야 하는 이유를 설명하세요.
4. 웹 레이아웃을 설계할 때 고려해야 할 사항을 설명하세요.
5. Flex와 Grid의 차이를 설명하세요.
6. DOM이 무엇인지 설명하세요.
7. HTML의 attribute와 JavaScript 객체의 property 차이를 설명하세요.
8. 동기 처리와 비동기 처리의 차이를 설명하세요.
9. Ajax의 개념과 특징을 설명하세요.
10. Ajax 또는 Fetch/Axios를 사용할 때의 장점과 단점을 설명하세요.
11. SPA와 전통적인 다중 페이지 웹 애플리케이션의 차이를 설명하세요.
12. React에서 `props`와 `state`의 차이를 설명하세요.
13. React에서 컴포넌트를 분리하는 기준은 무엇인가요?
14. React의 `useEffect`는 언제 사용하나요?
15. CORS 오류가 무엇이며 왜 발생하나요?

---

## 3. HTTP와 REST API

1. HTTP 요청과 응답은 어떤 구조로 이루어져 있나요?
2. GET, POST, PUT, PATCH, DELETE의 용도를 설명하세요.
3. PUT과 PATCH의 차이를 설명하세요.
4. HTTP 메서드의 멱등성이 무엇인지 설명하세요.
5. URL, URI, Endpoint의 차이를 설명하세요.
6. Path Variable과 Query Parameter의 차이를 설명하세요.
7. Request Body는 어떤 경우에 사용하나요?
8. REST API란 무엇이며 RESTful한 API란 무엇인가요?
9. JSON 형식을 사용하는 이유를 설명하세요.
10. 다음 상태 코드의 의미를 설명하세요.
    - 200
    - 201
    - 204
    - 400
    - 401
    - 403
    - 404
    - 409
    - 500
11. API 요청 데이터에 대한 검증은 왜 필요한가요?
12. API 명세서에는 어떤 내용이 포함되어야 하나요?
13. 클라이언트가 같은 주문 요청을 두 번 전송하면 어떻게 처리하겠습니까?

---

## 4. 데이터베이스

1. DBMS와 RDBMS의 차이를 설명하세요.
2. RDBMS와 NoSQL의 특징과 사용 사례를 설명하세요.
3. 테이블, 행, 열, 기본키, 외래키를 설명하세요.
4. 기본키와 유니크 제약조건의 차이를 설명하세요.
5. 정규화가 무엇이며 왜 필요한가요?
6. 1:N 관계와 N:M 관계를 설명하세요.
7. N:M 관계를 데이터베이스 테이블로 어떻게 표현하나요?
8. INNER JOIN과 LEFT JOIN의 차이를 설명하세요.
9. 여러 테이블의 데이터를 조회할 때 어떤 JOIN을 사용해야 하는지 판단하는 기준은 무엇인가요?
10. 서브쿼리와 JOIN의 차이와 장단점을 설명하세요.
11. 인덱스가 무엇이며 어떤 경우에 사용하나요?
12. 인덱스를 많이 생성하면 어떤 문제가 발생할 수 있나요?
13. 트랜잭션이 무엇인지 설명하세요.
14. 트랜잭션의 ACID 특성을 설명하세요.
15. COMMIT과 ROLLBACK의 차이를 설명하세요.
16. 데이터베이스 성능이 느릴 때 무엇을 확인하겠습니까?
17. SQL Injection이 무엇이며 어떻게 방지하나요?
18. 논리 삭제와 물리 삭제의 차이를 설명하세요.

### 상황형 질문

1. 주문과 주문 상세 테이블을 분리해야 하는 이유를 설명하세요.
2. 결제 처리 중 오류가 발생했다면 데이터의 일관성을 어떻게 유지하겠습니까?
3. 메뉴 이름을 검색하는 쿼리가 느리다면 어떤 부분을 확인하겠습니까?

---

## 5. Spring Boot·백엔드

1. MVC 패턴의 개념과 특징을 설명하세요.
2. Model, View, Controller의 역할을 설명하세요.
3. MVC 패턴의 장점과 단점을 설명하세요.
4. Spring MVC에서 요청이 처리되는 전체 흐름을 설명하세요.
5. `Controller`, `Service`, `Repository` 또는 `Mapper`의 역할을 설명하세요.
6. DTO, Entity, VO의 차이를 설명하세요.
7. DAO와 MyBatis Mapper의 관계를 설명하세요.
8. 웹 서버와 WAS의 차이를 설명하세요.
9. Tomcat의 역할을 설명하세요.
10. IoC와 DI가 무엇인지 설명하세요.
11. 생성자 주입을 사용하는 이유를 설명하세요.
12. Spring Bean이 무엇인지 설명하세요.
13. `@Controller`와 `@RestController`의 차이를 설명하세요.
14. `@RequestMapping`, `@GetMapping`, `@PostMapping`의 차이를 설명하세요.
15. `@RequestParam`, `@PathVariable`, `@RequestBody`의 차이를 설명하세요.
16. `@Component`, `@Service`, `@Repository`의 차이를 설명하세요.
17. `@Transactional`의 역할을 설명하세요.
18. 전역 예외 처리는 왜 필요하며 어떻게 구현할 수 있나요?
19. MyBatis의 `#{}`와 `${}` 차이를 설명하세요.
20. MyBatis 동적 SQL은 어떤 경우에 사용하나요?
21. JPA와 MyBatis의 차이를 설명하세요.
22. 서버 로그를 남겨야 하는 이유와 로그에 포함하면 안 되는 정보를 설명하세요.
23. 비밀번호를 데이터베이스에 그대로 저장하면 안 되는 이유를 설명하세요.
24. 세션 방식과 JWT 방식의 인증 차이를 설명하세요.
25. 인증과 인가의 차이를 설명하세요.

---

## 6. Git·협업

1. Git과 GitHub의 차이를 설명하세요.
2. `clone`, `add`, `commit`, `push`, `pull`의 역할을 설명하세요.
3. 브랜치를 사용하는 이유는 무엇인가요?
4. Merge와 Rebase의 차이를 설명하세요.
5. Git 충돌은 왜 발생하며 어떻게 해결하나요?
6. `.gitignore`의 역할을 설명하세요.
7. 좋은 커밋 메시지는 어떤 특징을 가져야 하나요?
8. 팀원이 작성한 코드를 검토할 때 무엇을 확인하겠습니까?
9. 다른 팀원의 코드와 API 형식이 맞지 않을 때 어떻게 해결하겠습니까?
10. Git에 DB 비밀번호나 API Key를 올렸다면 어떻게 대응해야 하나요?

---

# AI 개발 환경 및 기본 용어 질문지

AI는 모델을 직접 개발하는 수준보다, 개발자가 AI를 올바르게 활용할 수 있는지를 확인하는 정도가 적당합니다.

## 1. AI 기본 개념

1. 생성형 AI란 무엇인가요?
2. LLM이 무엇인지 설명하세요.
3. 생성형 AI의 프롬프트란 무엇인가요?
4. 토큰이 무엇이며 토큰 수가 중요한 이유는 무엇인가요?
5. 컨텍스트 윈도우가 무엇인가요?
6. AI의 할루시네이션이 무엇인지 설명하세요.
7. 임베딩이 무엇인지 아는 범위에서 설명하세요.

## 2. AI 개발 도구 활용

1. 개발 과정에서 생성형 AI를 어떤 용도로 사용해 보았나요?
2. AI가 작성한 코드를 그대로 사용하면 안 되는 이유는 무엇인가요?
3. AI가 생성한 코드가 올바른지 어떻게 검증하겠습니까?
4. 원하는 코드를 얻기 위한 좋은 프롬프트에는 어떤 정보가 포함되어야 하나요?
5. AI에게 오류 해결을 요청할 때 어떤 정보를 제공해야 하나요?
6. ChatGPT, GitHub Copilot, Gemini 등 AI 코딩 도구를 사용해 본 경험을 설명하세요.
7. AI에게 회사 소스 코드나 개인정보를 입력할 때 어떤 점을 주의해야 하나요?
8. AI가 제시한 라이브러리나 메서드가 실제로 존재하는지 어떻게 확인하나요?
9. AI를 사용해 테스트 코드를 작성한다면 어떤 순서로 진행하겠습니까?
10. AI를 사용한 부분과 본인이 직접 작성한 부분을 구분해서 설명할 수 있나요?

### AI 상황형 질문

1. AI가 작성한 코드가 실행은 되지만 이해되지 않는다면 어떻게 하겠습니까?
2. AI가 서로 다른 해결 방법을 제시한다면 어떤 기준으로 선택하겠습니까?
3. AI가 존재하지 않는 메서드나 잘못된 설정을 알려줬다면 어떻게 확인하겠습니까?
4. 프로젝트의 DB 접속 정보가 포함된 코드를 AI에게 질문해야 한다면 어떻게 처리하겠습니까?
5. AI가 만든 코드 때문에 보안 취약점이 발생하지 않도록 무엇을 확인하겠습니까?

---

# 프로젝트 경험 질문

실제 면접에서는 이 부분이 개념 질문보다 중요합니다.

1. 수행한 프로젝트를 1분 이내로 설명해 주세요.
2. 프로젝트에서 본인이 담당한 기능은 무엇인가요?
3. 본인이 직접 작성한 코드를 구체적으로 설명해 주세요.
4. 프로젝트의 전체 요청 처리 흐름을 설명해 주세요.
5. React에서 Spring Boot API를 호출하고 MySQL에 저장되는 과정을 설명해 주세요.
6. 프로젝트의 테이블 관계를 설명해 주세요.
7. API 주소와 요청·응답 형식은 어떻게 설계했나요?
8. 가장 해결하기 어려웠던 오류는 무엇이었나요?
9. 그 오류의 원인을 어떤 순서로 찾았나요?
10. 팀원과 의견이 달랐던 사례와 해결 방법을 설명해 주세요.
11. 개발 시간이 더 주어진다면 어떤 부분을 개선하고 싶나요?
12. 프로젝트에서 보안상 부족한 부분은 무엇인가요?
13. 테스트는 어떤 방식으로 진행했나요?
14. 예상하지 못한 중복 요청이나 잘못된 입력은 어떻게 처리했나요?
15. 본인이 작성한 코드 중 가장 자신 있는 부분을 설명해 주세요.

## 키오스크 프로젝트 특화 질문

1. 키오스크 사용자 화면과 관리자 화면을 분리한 이유는 무엇인가요?
2. 주문, 결제, 주문 상세 데이터는 어떤 관계로 설계했나요?
3. 결제 성공 후 주문 저장에 실패한다면 어떻게 처리해야 하나요?
4. 사용자가 결제 버튼을 연속으로 누르면 어떻게 처리하겠습니까?
5. 품절된 메뉴가 장바구니에 들어 있다면 어떻게 처리하겠습니까?
6. React의 더미 데이터를 실제 API 데이터로 교체한 과정을 설명하세요.
7. 키오스크 장치가 데이터베이스에 직접 접근하면 안 되는 이유는 무엇인가요?
8. 영수증 출력 요청이 발생했을 때 전체 처리 흐름을 설명하세요.
9. 장치가 오프라인이면 서버에서 어떻게 감지하고 기록할 수 있을까요?

---

# 면접 답변 예시 — ASAK 키오스크 프로젝트 기준

아래 답변은 `C:\ASAK-workspace`의 Product Bible, API 계약, 프론트·백엔드 연동 학습 기록을 기준으로 정리했습니다. 실제로 본인이 직접 구현하지 않은 부분은 면접에서 “설계 기준으로 확인한 내용”이라고 구분해 말합니다.

## 1. Java 핵심 답변

1. 클래스는 객체의 설계도이고, 객체는 그 설계도로 만든 실체입니다. 인스턴스는 특정 클래스에서 생성된 객체를 뜻합니다.
2. 캡슐화는 내부 데이터를 보호하고, 상속은 기존 기능을 확장하며, 다형성은 같은 타입으로 여러 구현을 다루는 것입니다. 추상화는 복잡한 구현을 숨기고 핵심 계약만 표현하는 것입니다.
3. 오버로딩은 같은 메서드명에 매개변수만 다르게 정의하는 것이고, 오버라이딩은 자식 클래스가 부모 메서드의 동작을 재정의하는 것입니다.
4. 추상 클래스는 공통 상태와 구현을 공유할 때, 인터페이스는 구현체가 지켜야 할 계약을 정의할 때 적합합니다.
5. `==`는 기본형의 값 또는 참조형의 주소를 비교하고, `equals()`는 객체의 논리적 동등성을 비교하도록 재정의할 수 있습니다.
6. List는 순서와 중복을 허용하고, Set은 중복을 허용하지 않으며, Map은 키와 값의 쌍으로 저장합니다.
7. 체크 예외는 컴파일 시 처리를 요구하고 언체크 예외는 실행 중 발생합니다. 예외는 무작정 숨기지 않고 원인과 사용자 응답을 분리해 처리합니다.
8. Stream은 필터링·변환·정렬을 선언적으로 표현해 가독성이 좋지만, 단순한 로직에는 일반 반복문이 더 읽기 쉬울 수 있습니다.
9. 의존성이 높은 코드는 한 변경이 여러 클래스에 전파되고 테스트가 어렵습니다. 인터페이스와 생성자 주입으로 의존성을 명시하겠습니다.

### Java 상황형 답변

- 가격 조건은 `filter`로 먼저 걸러내고 `sorted`로 가격순 정렬합니다. null 가격, 오름차순 여부, 결과가 없을 때의 처리도 함께 정의합니다.
- `NullPointerException`은 스택 트레이스의 발생 줄부터 확인하고, 해당 값의 생성·주입·DB 조회 흐름을 따라가며 재현합니다. 수정 후 같은 조건의 회귀 테스트를 추가합니다.
- 반복 코드는 공통 책임이 정말 같은지 확인한 뒤 메서드나 서비스로 추출합니다. 추출 후 기존 호출부와 테스트를 확인합니다.

## 2. 웹·React 핵심 답변

1. HTML은 구조, CSS는 표현과 배치, JavaScript는 동작과 상태를 담당합니다. 반응형 웹은 기기와 화면 크기에 맞게 레이아웃이 변하는 방식입니다.
2. DOM은 HTML을 브라우저가 객체 트리로 표현한 것입니다. JavaScript나 React는 상태 변화에 따라 필요한 화면을 갱신합니다.
3. 동기 처리는 앞 작업이 끝난 뒤 다음 작업을 하고, 비동기 처리는 네트워크처럼 오래 걸리는 작업을 기다리는 동안 다른 작업을 진행합니다.
4. SPA는 최초 로드 후 클라이언트가 라우팅과 화면 전환을 처리합니다. 초기 로딩과 상태 관리가 중요합니다.
5. React에서 props는 부모가 전달하는 읽기 전용 입력이고 state는 변경 시 렌더링을 일으키는 내부 상태입니다.
6. 컴포넌트는 독립적인 책임, 반복 UI, 별도 상태·이벤트 흐름을 기준으로 분리합니다.
7. `useEffect`는 API 조회, 이벤트 등록, 타이머처럼 렌더링 외부 시스템과 동기화할 때 사용합니다. 단순 계산에는 사용하지 않습니다.
8. CORS는 브라우저가 다른 출처의 요청을 제한하는 정책입니다. 서버가 허용할 출처·메서드·헤더를 설정해야 합니다.
9. Fetch/Axios를 사용할 때는 성공 응답만 처리하지 않고 로딩·빈 데이터·네트워크 오류·타임아웃·인증 만료까지 상태로 관리합니다.

## 3. HTTP·REST API 답변

1. HTTP 요청은 메서드·URL·헤더·본문, 응답은 상태 코드·헤더·본문으로 구성됩니다.
2. GET은 조회, POST는 생성, PUT은 전체 수정, PATCH는 부분 수정, DELETE는 삭제에 사용합니다.
3. Path Variable은 `/orders/{orderId}`처럼 리소스 식별에, Query Parameter는 검색·필터·페이지에 사용합니다.
4. REST API는 자원을 URI로 식별하고 HTTP 메서드와 상태 코드로 동작을 표현합니다. ASAK 계약은 응답을 `{ success, status, code, message, data }` envelope로 통일합니다.
5. 200은 성공, 201은 생성 성공, 204는 본문 없는 성공이며, 400·401·403·404·409·500은 각각 잘못된 요청·인증 실패·권한 부족·없음·충돌·서버 오류를 뜻합니다.
6. API 명세에는 URL, 메서드, 인증, 요청·응답 필드, 필수 여부, 상태 코드, 오류 코드, 예시를 기록합니다.
7. 중복 주문과 결제를 막기 위해 요청 식별자 또는 idempotency key, 서버 측 유니크 제약, 상태 검증을 함께 사용합니다. 프론트 버튼 비활성화만으로는 충분하지 않습니다.

## 4. 데이터베이스·Spring 답변

1. 기본키는 행을 식별하고 외래키는 테이블 사이의 관계를 표현합니다. 주문과 주문 상세는 1:N 관계입니다.
2. 주문 기본 정보와 반복되는 상품 정보를 분리하면 한 주문에 여러 상품·수량·주문 당시 가격을 저장할 수 있고 중복도 줄어듭니다.
3. INNER JOIN은 양쪽에 모두 있는 데이터만, LEFT JOIN은 왼쪽 기준 데이터를 모두 유지합니다. “연관 데이터가 없어도 기준 행을 보여줘야 하는가”로 판단합니다.
4. 인덱스는 조회를 빠르게 하지만 쓰기 작업 때 함께 갱신해야 하므로 무조건 많이 만들지 않습니다. 실행 계획과 실제 응답 시간을 비교합니다.
5. 트랜잭션은 여러 DB 작업을 하나의 작업 단위로 묶습니다. ACID는 원자성·일관성·격리성·지속성입니다.
6. SQL Injection은 사용자 입력으로 SQL 구조가 바뀌는 공격입니다. Prepared Statement와 MyBatis `#{}` 바인딩, 입력 검증, 최소 권한으로 방지합니다.
7. Spring에서 Controller는 HTTP 입출력, Service는 업무 규칙·트랜잭션, Repository/Mapper는 DB 접근을 담당합니다.
8. DTO는 전송용, Entity는 DB 매핑용입니다. 외부 요청을 Entity에 직접 바인딩하지 않고 DTO 검증 후 서비스에서 필요한 값만 사용합니다.
9. `@RestController`는 반환값을 응답 본문으로 만들고, `@RequestBody`는 JSON 본문, `@PathVariable`은 경로 값, `@RequestParam`은 쿼리 값을 받습니다.
10. `@Transactional`은 성공 시 커밋하고 예외 시 롤백하도록 작업 범위를 묶습니다. 외부 결제 승인과 DB 트랜잭션은 별도 시스템이므로 실패 시 보상 취소·재처리 정책도 필요합니다.
11. MyBatis에서 `#{}`는 안전하게 값을 바인딩하고 `${}`는 문자열 치환이므로 사용자 입력에 사용하지 않습니다.
12. 전역 예외 처리는 `@RestControllerAdvice` 등으로 오류 형식을 통일하고 Controller의 중복 처리를 줄이는 방식입니다.

## 5. ASAK 프로젝트 1분 소개

“ASAK은 고객용 키오스크와 관리자 운영 화면을 연결한 샐러드·샌드위치 주문 서비스입니다. 고객은 주문 방식인 매장 식사 또는 포장을 선택하고, 메뉴·옵션·제외 재료를 고른 뒤 장바구니와 결제를 진행합니다. 관리자는 주문을 접수·조리·완료 상태로 변경하고 실시간 주문 보드와 TTS 안내를 사용할 수 있습니다. 프론트엔드와 백엔드를 REST API로 연결하고, DB에서는 주문·주문 상세·결제·메뉴 데이터를 관계로 관리하는 구조입니다. 제가 답변할 때는 실제로 담당한 화면과 파일을 구체적으로 덧붙이겠습니다.”

## 6. ASAK 실제 데이터 흐름 답변

### 고객 주문 흐름

“화면 흐름은 Home에서 `orderType`을 선택하고 Menu List, Menu Detail, Cart, 주문 생성, Payment, Complete 순서로 이동합니다. 장바구니에서 `validateCart()`를 수행한 뒤 `POST /api/kiosk/orders`를 호출합니다. 서버는 수량, 메뉴 판매 상태, 옵션 필수·최소·최대 선택, 옵션 품절, 제외 재료 영향을 검증하고 가격을 다시 계산한 후 주문을 저장합니다. 응답으로 `orderId`, `orderNo`, `orderStatus`, `paymentStatus`, `totalAmount`를 받아 결제 화면으로 이동합니다.”

### 주문·결제 관계

“주문은 주문 기본 정보이고 주문 상세는 한 주문에 포함된 상품별 수량과 가격입니다. 주문과 주문 상세는 1:N입니다. 결제는 주문과 연결되지만 승인 상태와 승인 금액을 별도로 관리합니다. 결제 요청은 `POST /api/kiosk/payments`로 보내고, `orderId`, `paymentMethodCode`, `idempotencyKey`를 전달합니다. 결제 성공은 `APPROVED`, 실패는 `FAILED`로 관리하며, 주문 완료 상태와 결제 승인 상태를 같은 의미로 혼동하지 않습니다.”

### 관리자 주문 처리

“관리자 주문 목록은 `useOrdersQuery`가 필터·페이지 조건을 관리하고 `ordersApi`를 거쳐 `GET /api/admin/orders`를 호출합니다. 백엔드는 Controller, Service, Mapper를 거쳐 `orders`, `payment`, `order_item`, `common_code`를 조회하고 `PageResult` 형태로 응답합니다. 주문 상태는 기본적으로 `RECEIVED → PREPARING → COMPLETED` 순서입니다. 완료 PATCH가 성공한 뒤에만 TTS를 실행하고, 네트워크 오류가 나면 기존 주문 카드를 지우지 않고 오류를 표시합니다.”

## 7. ASAK 특화 질문 답변

1. **키오스크와 관리자 화면을 분리한 이유**: 고객 화면은 빠르고 단순한 주문 경험이 목적이고, 관리자 화면은 주문 상태·메뉴·매출·운영 기능이 목적입니다. 역할과 권한, 화면 상태가 다르므로 분리했습니다.
2. **메뉴 API**: 현재 계약은 `GET /api/kiosk/menuList?categoryId=...`, 상세는 `GET /api/kiosk/menuDetail/{menuId}`입니다. 메뉴 응답은 `menuId`, `categoryId`, `name`, `price`, `isSoldOut`, `isOrderable` 등을 사용하며, DB snake_case와 API camelCase를 구분합니다.
3. **가격 검증**: 클라이언트의 `totalAmount`를 신뢰하지 않고 서버가 메뉴·옵션 가격을 다시 계산합니다. 서버 계산 금액과 결제 승인 금액이 다르면 `ORDER_AMOUNT_MISMATCH` 또는 가격 변경 흐름으로 처리합니다.
4. **결제 성공 후 주문 저장 실패**: 결제 승인과 주문 저장은 외부 결제 시스템과 DB의 작업이므로 하나의 트랜잭션처럼 가정하지 않습니다. 주문 상태를 재조회하고 저장 재처리 또는 결제 취소 보상 절차를 실행하며, 사용자에게 성공으로 오인될 메시지를 먼저 보여주지 않습니다.
5. **결제 버튼 연속 클릭**: 프론트는 처리 중 버튼을 비활성화하고, 서버는 `idempotencyKey`, `PAYMENT_IN_PROGRESS`, `PAYMENT_ALREADY_APPROVED`, DB 제약으로 중복 결제를 막습니다.
6. **품절 메뉴가 장바구니에 있음**: 장바구니 표시 시점과 주문 생성 시점 모두 확인합니다. 서버에서 메뉴·옵션·재료의 품절 여부를 다시 검증하고 `MENU_SOLD_OUT`, `OPTION_ITEM_SOLD_OUT` 등 오류를 내려 삭제 또는 옵션 수정을 안내합니다.
7. **더미 데이터를 실제 API로 교체**: 먼저 mock 필드와 API 계약을 비교하고, API 모듈·응답 매핑·로딩·빈 상태·오류 상태를 분리합니다. 실제 ASAK 학습 기록상 키오스크 메뉴 화면은 한 시점에 `kiosk.json` 목업을 직접 사용했고, API 경로·프록시·응답 필드 정합성을 먼저 맞춰야 하는 상태였습니다. 따라서 “완전히 연결했다”고 과장하지 않고 연결 준비와 확인 범위를 구분해 설명합니다.
8. **키오스크가 DB에 직접 접근하면 안 되는 이유**: DB 자격증명 노출, 권한 우회, SQL Injection, 스키마 노출 위험이 있습니다. 키오스크는 서버 API만 호출하고 서버가 인증·검증·가격 계산·권한을 담당해야 합니다.
9. **영수증 출력**: 결제 승인과 주문 정보를 확인한 뒤 출력 데이터를 만들고 장치 또는 모의 프린터에 전달합니다. 성공·실패·재출력 여부를 기록하며, 출력 실패가 결제 실패로 잘못 표시되지 않도록 상태를 분리합니다.
10. **장치 오프라인 감지**: 키오스크가 주기적으로 heartbeat를 보내고 서버가 마지막 수신 시각을 저장합니다. 일정 시간 동안 heartbeat가 없으면 오프라인으로 표시하고, 복구 시각과 장애 이력을 기록합니다.
11. **주문 상태 중복 완료**: 완료 버튼을 로딩 중 비활성화하고 서버는 이미 `COMPLETED`이면 현재 상태를 반환하는 멱등 처리를 합니다. TTS와 매출 반영 이벤트는 중복 발생하지 않도록 차단합니다.
12. **Polling 충돌**: 이전 요청이 끝나기 전에는 새 요청을 보내지 않고, 컴포넌트 해제 시 interval을 정리합니다. 오래된 응답이 최신 화면을 덮어쓰지 않도록 `updatedAt` 또는 요청 시점을 비교합니다.
13. **다른 관리자가 먼저 상태 변경**: 서버의 현재 상태를 기준으로 응답하고 프론트 목록을 다시 동기화합니다. 내 화면의 오래된 상태만 보고 성공 처리하지 않습니다.
14. **네트워크 오류**: 기존 주문 카드와 마지막 정상 데이터를 유지하고 Error Toast를 표시합니다. 오류 한 번으로 화면을 빈 목록으로 바꾸지 않습니다.
15. **보안상 개선점**: 인증·인가, 비밀번호 해시, HTTPS, 입력 검증, 민감정보 로그 차단, 결제 위변조 방지, 중복 요청 방어를 우선 보완하겠습니다.

## 8. Git·협업 및 AI 답변

1. Git은 버전 관리 도구이고 GitHub는 원격 저장소와 협업 기능을 제공합니다. 브랜치는 기능 단위로 분리하고, 충돌이 나면 양쪽 의도를 확인한 뒤 테스트하고 병합합니다.
2. API 형식이 맞지 않으면 프론트 코드만 임의로 고치지 않고 Controller·DTO·실제 응답·Product Bible 계약을 함께 대조합니다. 필드명, HTTP 메서드, 상태 코드, 오류 형식을 합의하고 최소 범위만 수정합니다.
3. AI는 코드 설명, 오류 원인 후보, 테스트 케이스, 문서 초안에 활용할 수 있지만 생성 코드를 그대로 사용하지 않습니다. 실제 저장소·공식 문서·실행 결과로 검증하고, 본인이 설명할 수 없는 코드는 반영하지 않습니다.
4. AI에 질문할 때는 재현 절차와 오류 로그를 제공하되 DB 비밀번호·토큰·개인정보는 제거합니다. 답변에 존재하지 않는 API나 메서드가 섞이지 않았는지 의존성 파일과 공식 문서로 확인합니다.

## 9. 면접에서 사실과 계획을 구분하는 표현

- 실제 확인한 내용: “ASAK 학습 기록과 코드 대조 결과, 관리자 주문 목록은 API·DB 조회 흐름이 확인되었습니다.”
- 설계 계약: “Product Bible 기준으로 주문 생성은 `POST /api/kiosk/orders`이고 서버가 가격을 재계산하도록 정의되어 있습니다.”
- 미완성 또는 확인 필요: “키오스크 메뉴·결제는 확인 시점에 목업 또는 API 골격 상태였으므로, 완성된 실서비스 연동이라고 말하지 않겠습니다.”
- 개선 계획: “다음 단계로 API 경로, Vite 프록시, 응답 envelope, 서버 검증을 맞춘 뒤 통합 테스트로 확인하겠습니다.”

## 마무리 답변

“저는 기능이 화면에서 보이는 것만으로 끝내지 않고, 프론트 요청부터 Controller·Service·Mapper·DB, 그리고 오류와 재시도까지 데이터 흐름으로 확인하려고 합니다. AI를 사용하더라도 실제 코드와 계약, 실행 결과를 기준으로 검증하고 제가 이해하지 못한 코드는 사용하지 않겠습니다.”
